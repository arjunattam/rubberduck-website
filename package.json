{
  "name": "gatsby-starter-default",
  "description": "Gatsby default starter",
  "version": "1.0.0",
  "author": "Kyle Mathews <mathews.kyle@gmail.com>",
  "dependencies": {
    "bootstrap": "^4.0.0",
    "devicon": "https://github.com/konpa/devicon",
    "gatsby": "^1.9.223",
    "gatsby-link": "^1.6.38",
    "gatsby-plugin-catch-links": "^1.0.19",
    "gatsby-plugin-react-helmet": "^2.0.6",
    "gatsby-source-filesystem": "^1.5.27",
    "gatsby-transformer-remark": "^1.7.37",
    "medium-zoom": "^0.4.0",
    "react-ga": "^2.4.1",
    "react-helmet": "^5.2.0",
    "reactstrap": "5.0.0-beta"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf public/",
    "build": "npm run clean && gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --write 'src/**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "gsclean": "gsutil -m rm \"gs://www.rubberduck.io/**\"",
    "deploy": "gsutil -m cp -r public/* gs://www.rubberduck.io",
    "permissions": "gsutil -m acl ch -u AllUsers:R \"gs://www.rubberduck.io/**\"",
    "push": "npm run build && npm run deploy && npm run permissions"
  },
  "devDependencies": {
    "prettier": "^1.11.1"
  }
}
