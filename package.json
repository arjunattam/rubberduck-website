{
  "name": "gatsby-starter-default",
  "description": "Gatsby default starter",
  "version": "1.0.0",
  "author": "Kyle Mathews <mathews.kyle@gmail.com>",
  "dependencies": {
    "bootstrap": "^4.0.0",
    "devicon": "https://github.com/konpa/devicon",
    "gatsby": "^1.9.223",
    "gatsby-link": "^1.6.38",
    "gatsby-plugin-react-helmet": "^2.0.6",
    "react-ga": "^2.4.1",
    "react-helmet": "^5.2.0",
    "reactstrap": "5.0.0-beta"
  },
  "keywords": ["gatsby"],
  "license": "MIT",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --write 'src/**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gsutil -m cp -r public/* gs://www.rubberduck.io",
    "permissions":
      "gsutil -m acl ch -u AllUsers:R \"gs://www.rubberduck.io/**\"",
    "push": "gatsby build && npm run deploy && npm run permissions"
  },
  "devDependencies": {
    "prettier": "^1.11.1"
  }
}
